# ============================================
# Obsidian — personal UI state
# ============================================
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/workspace.json.bak
.obsidian/cache
.obsidian/app.json.bak
.trash/

# ============================================
# Claudian plugin — ignore entire plugin state folder
# ============================================
.claudian/

# ============================================
# Claude Code — keep skills/commands/CLAUDE.md tracked, ignore local settings
# ============================================
.claude/settings.local.json
.claude.json.backup

# ============================================
# Vault runtime state markers (ephemeral, machine-local, never content)
# ============================================
# Written by /sync (watermark); read by subsequent /sync runs to find changed files.
.last_sync
# Written by /sync all; read and deleted by /graph to force a full rebuild after brute-force sync.
.sync_all_fresh
# Written by /status Step 7.6 and /prune Stage 4.5 on closure; read and cleared by /graph last
# to re-extract adjacency for neighbor theses that cross-referenced the now-archived thesis.
.graph_invalidations
# Written by every state-modifying skill at Step 0.1 and released at skill end (preflight Procedure 1).
# Token-keyed concurrency lock — committing a live lock would block every machine that pulls the repo.
.vault-lock
.vault-lock.*
# Written by /rename Step 5.5 on post-mv wikilink-rewrite failure (per-ticker).
# Hard-blocks every ticker-scoped skill on TICKER until /rename repair re-run clears it; never persistent.
.rename_incomplete.*

# ============================================
# Secrets — NEVER commit these
# ============================================
.env
.env.local
.env.*.local
*.key
*.pem
credentials.json
secrets.json

# ============================================
# macOS system files
# ============================================
.DS_Store
._*
.Spotlight-V100
.Trashes

# ============================================
# Windows system files
# ============================================
Thumbs.db
desktop.ini

# ============================================
# Temp and backup files
# ============================================
*.tmp
*.swp
*.swo
*~
~$*
.obsidian/plugins/claudian/data.json
.obsidian/plugins/claudian/data.json
.claude/settings.json
