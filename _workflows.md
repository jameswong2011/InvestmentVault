---
publish: false
date: 2026-07-22
tags: [meta, workflows, registry]
---
# Workflows Registry

> Auto-generated by `.claude/workflows/_generate_registry.mjs` — re-run after adding or editing a workflow. Workflows are multi-agent orchestrations (heavier than skills); each lives at `.claude/workflows/<name>.js` and is invoked by asking Claude to run it. First run prompts a usage warning (they can spawn many agents); watch progress with `/workflows`.

## Available (8)

| Workflow                     | What it does                                                                                                                                                                                                                                                                | When to use                                                                                                                                                               |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `portfolio-conviction-audit` | Audit every thesis for conviction-evidence mismatch and silently-fired Conviction Triggers (via the conviction-audit skill); rank the book by over-conviction + unactioned triggers and feed /status.                                                                       | Periodic conviction hygiene — find over-convicted names and triggers that fired without action. Read-only unless persist:true (writes _followups only; never conviction). |
| `portfolio-correlation`      | Map every thesis's dependency fingerprint (via the dependency-map skill), then find names whose bull cases secretly rest on the same variable — correlated bets the market prices independently.                                                                            | Find hidden cross-name correlation / concentration in the book. Read-only unless persist:true.                                                                            |
| `portfolio-macro-exposure`   | Tag every thesis's implicit macro bets (via the macro-exposure skill), then find where the book is concentrated in a single macro variable dressed up as diversification.                                                                                                   | Portfolio macro-concentration audit. Read-only unless persist:true.                                                                                                       |
| `portfolio-retro`            | Portfolio-scale retrospective (via the retro skill's method): fan out the per-ticker price/news/earnings overlay with throttled concurrency (fixes the ~126-query rate-limit that makes unscoped /retro return data-gap), then rank trade ideas by the narrative-price gap. | Full-portfolio retro without the WebSearch rate-limit. args.window = 1w\|1m\|1q (default 1w). Read-only unless persist:true.                                              |
| `portfolio-scenario`         | Model a named macro event across the whole book in parallel (via the scenario skill's impact method): fan out per-thesis impact assessment, rank winners/losers + transmission channels, and (persist) write Major-impact Log entries + one scenario note.                  | Model a specific macro event ("Fed cuts 150bps", "Taiwan blockade") across all theses fast. Requires args.event. Read-only unless persist:true.                           |
| `portfolio-stress-test`      | Adversarially stress-test every active/monitoring thesis, verify each weakness with independent skeptics, and rank the portfolio by confirmed downside risk.                                                                                                                | Periodic portfolio-wide risk sweep. Heavy (one stress agent per thesis + up to 3 verifiers per at-risk name). Run deliberately, not routinely.                            |
| `portfolio-supply-chain`     | Map each thesis's value-chain position (via the value-chain skill), then stitch the book into one supply-chain graph — shared suppliers, shared customers, and cross-thesis single points of failure.                                                                       | Portfolio value-chain / concentration map. Read-only unless persist:true (writes a note + optional Canvas).                                                               |
| `vault-contradictions`       | Extract every thesis's load-bearing assumptions (via the assumptions skill), find cross-thesis clashes (one thesis's bull premise = another's bear premise), and adversarially verify each is a real contradiction, not a framing difference.                               | Portfolio coherence audit — where do two positions bet opposite ways on the same industry claim. Read-only unless persist:true.                                           |

## Details

### portfolio-conviction-audit
- **File**: `.claude/workflows/portfolio-conviction-audit.js`
- **Does**: Audit every thesis for conviction-evidence mismatch and silently-fired Conviction Triggers (via the conviction-audit skill); rank the book by over-conviction + unactioned triggers and feed /status.
- **Phases**: Enumerate → Audit → Rank → Persist
- **Invoke**: ask *"run the portfolio-conviction-audit workflow"* — or `Workflow({name:"portfolio-conviction-audit"})` / `Workflow({scriptPath:".claude/workflows/portfolio-conviction-audit.js"})`

### portfolio-correlation
- **File**: `.claude/workflows/portfolio-correlation.js`
- **Does**: Map every thesis's dependency fingerprint (via the dependency-map skill), then find names whose bull cases secretly rest on the same variable — correlated bets the market prices independently.
- **Phases**: Enumerate → Extract → Correlate → Persist
- **Invoke**: ask *"run the portfolio-correlation workflow"* — or `Workflow({name:"portfolio-correlation"})` / `Workflow({scriptPath:".claude/workflows/portfolio-correlation.js"})`

### portfolio-macro-exposure
- **File**: `.claude/workflows/portfolio-macro-exposure.js`
- **Does**: Tag every thesis's implicit macro bets (via the macro-exposure skill), then find where the book is concentrated in a single macro variable dressed up as diversification.
- **Phases**: Enumerate → Tag → Aggregate → Persist
- **Invoke**: ask *"run the portfolio-macro-exposure workflow"* — or `Workflow({name:"portfolio-macro-exposure"})` / `Workflow({scriptPath:".claude/workflows/portfolio-macro-exposure.js"})`

### portfolio-retro
- **File**: `.claude/workflows/portfolio-retro.js`
- **Does**: Portfolio-scale retrospective (via the retro skill's method): fan out the per-ticker price/news/earnings overlay with throttled concurrency (fixes the ~126-query rate-limit that makes unscoped /retro return data-gap), then rank trade ideas by the narrative-price gap.
- **Phases**: Universe → Overlay → Rank → Persist
- **Invoke**: ask *"run the portfolio-retro workflow"* — or `Workflow({name:"portfolio-retro"})` / `Workflow({scriptPath:".claude/workflows/portfolio-retro.js"})`

### portfolio-scenario
- **File**: `.claude/workflows/portfolio-scenario.js`
- **Does**: Model a named macro event across the whole book in parallel (via the scenario skill's impact method): fan out per-thesis impact assessment, rank winners/losers + transmission channels, and (persist) write Major-impact Log entries + one scenario note.
- **Phases**: Enumerate → Assess → Synthesize → Persist
- **Invoke**: ask *"run the portfolio-scenario workflow"* — or `Workflow({name:"portfolio-scenario"})` / `Workflow({scriptPath:".claude/workflows/portfolio-scenario.js"})`

### portfolio-stress-test
- **File**: `.claude/workflows/portfolio-stress-test.js`
- **Does**: Adversarially stress-test every active/monitoring thesis, verify each weakness with independent skeptics, and rank the portfolio by confirmed downside risk.
- **Phases**: Enumerate → Stress → Verify → Synthesize → Persist
- **Invoke**: ask *"run the portfolio-stress-test workflow"* — or `Workflow({name:"portfolio-stress-test"})` / `Workflow({scriptPath:".claude/workflows/portfolio-stress-test.js"})`

### portfolio-supply-chain
- **File**: `.claude/workflows/portfolio-supply-chain.js`
- **Does**: Map each thesis's value-chain position (via the value-chain skill), then stitch the book into one supply-chain graph — shared suppliers, shared customers, and cross-thesis single points of failure.
- **Phases**: Enumerate → Position → Stitch → Persist
- **Invoke**: ask *"run the portfolio-supply-chain workflow"* — or `Workflow({name:"portfolio-supply-chain"})` / `Workflow({scriptPath:".claude/workflows/portfolio-supply-chain.js"})`

### vault-contradictions
- **File**: `.claude/workflows/vault-contradictions.js`
- **Does**: Extract every thesis's load-bearing assumptions (via the assumptions skill), find cross-thesis clashes (one thesis's bull premise = another's bear premise), and adversarially verify each is a real contradiction, not a framing difference.
- **Phases**: Enumerate → Extract → Pair → Verify → Persist
- **Invoke**: ask *"run the vault-contradictions workflow"* — or `Workflow({name:"vault-contradictions"})` / `Workflow({scriptPath:".claude/workflows/vault-contradictions.js"})`
